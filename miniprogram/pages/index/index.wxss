/* tabBar 是原生层，会盖住页面底部；这里预留出它和 home indicator 的高度 */
.index-page {
  padding-bottom: calc(160rpx + env(safe-area-inset-bottom));
}

.brand-row {
  display: flex;
  align-items: flex-start;
}

.brand-lockup {
  font-family: "STHeiti", "PingFang SC", "Microsoft YaHei", sans-serif;
  font-size: 40rpx;
  font-weight: 1000;
  letter-spacing: -4rpx;
  -webkit-text-stroke: 1rpx #101619;
}

.brand-meta {
  margin: 8rpx 0 0 16rpx;
  font-size: 24rpx;
  font-weight: 900;
  line-height: 1.4;
}

.sticker {
  display: inline-block;
  margin-top: 22rpx;
  padding: 14rpx 20rpx;
  background: #ddff32;
  box-shadow: 6rpx 6rpx 0 #101619;
  font-size: 24rpx;
  font-weight: 1000;
  line-height: 1.05;
  transform: rotate(-4deg);
}

.display-art {
  display: block;
  width: 100%;
  /* 图片自带约 37rpx 的米色留白，负边距收掉，否则和分隔线之间会空出一大段 */
  margin: 10rpx 0 -30rpx;
}

.marker-line {
  width: 100%;
  height: 14rpx;
  margin: 10rpx 0 14rpx;
  background: #1648d8;
  clip-path: polygon(0 20%, 100% 0, 98% 72%, 2% 100%);
}

.score-card,
.meter-card,
.input-card {
  position: relative;
  margin-top: 40rpx;
  border: 5rpx solid #101619;
  border-radius: 28rpx;
  background: #fffdf8;
}

.score-card {
  padding: 22rpx 24rpx 14rpx;
  box-shadow: 10rpx 10rpx 0 #101619;
}

.card-tab {
  position: absolute;
  top: -24rpx;
  left: 16rpx;
  padding: 9rpx 22rpx;
  border-radius: 999rpx;
  background: #101619;
  color: #ffffff;
  font-size: 24rpx;
  font-weight: 1000;
}

.timer {
  color: #1648d8;
  font-family: "Arial Black", Arial, sans-serif;
  font-size: 92rpx;
  font-weight: 1000;
  letter-spacing: -4rpx;
  line-height: 0.95;
  text-align: center;
  transform: skewX(-5deg);
  font-variant-numeric: tabular-nums;
  -webkit-text-stroke: 3rpx #1648d8;
  text-shadow: -2rpx 0 #1648d8, 2rpx 0 #1648d8, 0 -2rpx #1648d8, 0 2rpx #1648d8;
}

.timer-idle {
  color: #a9a29a;
  -webkit-text-stroke-color: #a9a29a;
  text-shadow: none;
}

.timer-caption {
  margin-top: 8rpx;
  overflow: hidden;
  font-size: 22rpx;
  font-weight: 900;
  text-align: center;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.meter-card {
  padding: 20rpx;
}

.meter-card-hover {
  background: #f5f1e8;
}

.meter-title {
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-family: "STHeiti", "PingFang SC", "Microsoft YaHei", sans-serif;
  font-size: 24rpx;
  font-weight: 1000;
  -webkit-text-stroke: 1rpx #101619;
  text-shadow: 1rpx 0 #101619, 0 1rpx #101619;
}

.meter-score {
  font-family: "Arial Black", Arial, sans-serif;
  font-size: 48rpx;
  font-weight: 1000;
  font-variant-numeric: tabular-nums;
  -webkit-text-stroke: 2rpx #101619;
}

.meter-track {
  height: 34rpx;
  margin-top: 10rpx;
  padding: 5rpx;
  border: 4rpx solid #101619;
  border-radius: 999rpx;
}

.meter-fill {
  width: 0%;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #ddff32, #ffe53b 52%, #ff543c);
  transition: width 280ms ease-out;
}

.meter-score.level-warm {
  color: #f0810a;
  -webkit-text-stroke-color: #f0810a;
}

.meter-score.level-hot {
  color: #ff543c;
  -webkit-text-stroke-color: #ff543c;
}

.meter-labels {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16rpx;
  margin-top: 10rpx;
  font-size: 24rpx;
  font-weight: 900;
}

.meter-hint {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.meter-why {
  flex: none;
  color: #6d6862;
}

.input-card {
  /* 额外补偿 .card-tab 向上溢出的 24rpx，避免标签压到上一张卡片 */
  margin-top: 62rpx;
  padding: 26rpx 16rpx 16rpx;
}

.input-card .field {
  margin: 0;
  border: 3rpx solid #101619;
  border-radius: 18rpx;
  background: #f5f1e8;
}

.hero-button {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 40rpx;
  margin-top: 20rpx;
  min-height: 104rpx;
  font-size: 38rpx;
  font-style: italic;
  letter-spacing: -3rpx;
}

.hero-button-hover {
  box-shadow: 2rpx 2rpx 0 #101619;
  transform: translate(6rpx, 6rpx);
}

.hero-button text {
  display: grid;
  width: 58rpx;
  height: 58rpx;
  place-items: center;
  border-radius: 50%;
  background: #ddff32;
  color: #101619;
  font-size: 34rpx;
}

.consent-mask {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 48rpx;
  background: rgba(16, 22, 25, 0.55);
}

.consent-dialog {
  position: relative;
  width: 100%;
  max-width: 600rpx;
  box-sizing: border-box;
  padding: 44rpx 36rpx 36rpx;
  border: 6rpx solid #101619;
  border-radius: 32rpx;
  background: #fffdf8;
  box-shadow: 14rpx 14rpx 0 #101619;
  animation: consent-pop 180ms ease-out;
}

@keyframes consent-pop {
  from {
    opacity: 0;
    transform: translateY(26rpx) scale(0.96);
  }
  to {
    opacity: 1;
    transform: none;
  }
}

.consent-tab {
  position: absolute;
  top: -24rpx;
  left: 28rpx;
  padding: 9rpx 22rpx;
  border-radius: 999rpx;
  background: #101619;
  color: #ffffff;
  font-size: 24rpx;
  font-weight: 1000;
}

.consent-title {
  font-size: 46rpx;
  font-weight: 1000;
  letter-spacing: -2rpx;
  line-height: 1.1;
  -webkit-text-stroke: 2rpx #101619;
}

.consent-body {
  margin-top: 20rpx;
  color: #41423f;
  font-size: 26rpx;
  font-weight: 700;
  line-height: 1.65;
}

.consent-confirm {
  margin-top: 34rpx;
}

.consent-decline {
  margin-top: 18rpx;
}

.consent-confirm-hover,
.consent-decline-hover {
  box-shadow: 2rpx 2rpx 0 #101619;
  transform: translate(6rpx, 6rpx);
}

