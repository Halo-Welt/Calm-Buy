# Calm Buy · 微信小程序任务计划书

> 交给 GPT / Codex 执行用。按阶段推进，每阶段有明确交付物与验收标准。  
> 工作目录建议：`/Users/liuxinyutencent/Desktop/Calm-Buy`  
> 产品临时名：**Calm Buy**（可改名，勿在未确认前大改品牌文案结构）

---

## 0. 一句话产品

用户一时上头想买东西时，用 1–2 分钟对话拆清「真正想要什么」，结合优质评测摘要，给出 **劝停 / 可买 / 先验证** 判决；仅在置信度足够且存在更好手段时给替代品。目标是 **少乱买**，不是导购成交。

---

## 1. 产品原则（实现时不可违背）

1. **反导购**：默认立场是帮用户冷静，不是促成下单。
2. **先需求后商品**：未确认「需求原子」前，不得推荐商品或替代品。
3. **替代有门槛**：只有「置信度高 + 存在更好手段」才给替代；纯劝停时不硬塞商品。
4. **证据可核对**：每个需求原子必须对应一句用户确认过的话；评测结论要带来源。
5. **置信度诚实**：低置信度时说「不确定」，引导补问或「冷却 48 小时」，禁止装懂。
6. **轻量**：MVP 不做账号体系、不做支付、不做跨平台比价爬虫、不做社交 feed。

---

## 2. MVP 范围

### 2.1 做（In Scope）

| 模块 | 说明 |
|------|------|
| 微信小程序前端 | 对话式主流程 + 结果卡 |
| 欲望输入 | 支持：商品名文本 / 粘贴链接（当文本）/ 简单描述 |
| 需求拆解对话 | 固定题库 + LLM 追问，≤ 5 轮问完主需求 |
| 判决引擎 | 规则 + LLM：劝停 / 可买 / 先验证 + 置信度 |
| 评测摘要 | MVP：用户可粘贴 1 条评测链接或评测原文；系统摘要。另内置少量「示例信源说明」 |
| 替代品 | 条件触发，最多 2 个，必须标注满足哪个需求原子 |
| 结果页 | 可复制文案 / 可截图的「解剖报告」卡 |
| 冷却提醒 | 本地记录「待冷静」清单（storage），支持标记已冷静/已放弃 |

### 2.2 不做（Out of Scope · MVP）

- 登录 / 手机号 / 用户中心
- 支付、分销、电商跳转成交闭环
- 全网自动爬取评测、实时比价、跨店库存
- 图像识别商品（可后续）
- 社区、分享裂变后台
- 原生插件复杂能力（如微信好物圈）

### 2.3 成功标准（产品）

- 用户能在 **3 分钟内** 走完一次完整流程并看到结果卡
- 结果卡必含：真实需求一句、判决、依据、置信度
- 「劝停且不给替代」与「给替代」两条路径都能演示
- 界面语气像聪明损友，不像说教家长

---

## 3. 核心用户流程

```
启动
  → 输入「好想买什么」（文本）
  → 系统确认理解的商品/品类
  → 需求拆解问答（最多 5 问）
  → 用户确认：「你真正想要的是 ___」
  → （可选）粘贴评测原文或链接文本
  → 生成判决
  → 结果卡
       ├─ 劝停（无替代 或 仅非购物建议）
       ├─ 先验证（租/试用/等评测/等 48h）
       └─ 可买（少见）或 替代品（1–2 个）
  → 可加入「冷静清单」（本地）
```

---

## 4. 需求原子枚举（写死进系统）

实现时用固定枚举，便于规则与统计（中文展示名 + id）：

| id | 展示名 | 含义 |
|----|--------|------|
| `identity` | 身份/被看见 | 虚荣、人设、社交货币 |
| `novelty` | 新奇体验 | 尝鲜、早期采用者快感 |
| `utility` | 场景功能 | 明确场景下的实用能力 |
| `anxiety` | 落后焦虑 | FOMO、信息焦虑 |
| `emotion` | 情绪奖励 | 解闷、自我犒劳、报复性消费 |
| `belong` | 归属/跟风 | 朋友都有、圈子标配 |
| `collect` | 收藏占有 | 拥有欲本身 |

规则提示：

- `identity` / `emotion` / `anxiety` / `belong` 默认更倾向 **劝停或非购物满足**，慎给硬件替代。
- `utility` 才是替代品主战场。
- `novelty` 优先「先验证」（租、体验店、借朋友的）。

---

## 5. 追问脚本（给 LLM 的产品约束）

### 5.1 目标

在 ≤ 5 轮内确认 **主需求原子（1 个）** + **次需求（可选 1 个）** + **使用场景一句话**。

### 5.2 必问信息（可合并提问）

1. 主要在什么场景用？多久用一次？
2. 如果没有任何朋友知道你买了，还想买吗？（测 identity）
3. 你更想解决的是「具体不方便」还是「心里痒」？
4. 预算和能否接受「更丑但够用」的替代？
5. 是否已经对比过别的方案？卡在哪里？

### 5.3 确认句模板

> 我理解你真正想要的是：**[场景下的体验/感受]**，核心更像 **[原子中文名]**，而不是「拥有 [商品名]」本身。对吗？

用户否定则再问 1 轮，仍不清则置信度降为 `low`。

---

## 6. 判决与置信度规则

### 6.1 判决类型

| 判决 | 含义 | 何时用 |
|------|------|--------|
| `stop` | 劝停 | 商品与主需求低匹配，或主需求非购物可满足 |
| `wait` | 先验证 / 冷却 | 新奇、信息不足、评测两极、高价未体验 |
| `buy` | 可以买 | 主需求 utility 且高匹配，评测共识正面，用户场景清晰 |
| `replace` | 建议替代 | 主需求清晰，当前商品非最短路径，能提出更好手段 |

注意：`replace` 是「判决结果形态」，UI 上仍先亮「不建议买当前这个」，再给替代。

### 6.2 置信度

| 级 | 条件 | 行为 |
|----|------|------|
| `high` | 用户确认需求句 + 场景清晰 +（有评测摘要或品类常识足够） | 给明确判决；可给替代 |
| `medium` | 需求确认了但场景模糊或无评测 | 倾向 `wait`；替代最多 1 个且标注不确定 |
| `low` | 用户否定确认句或前后矛盾 | 不给替代；列出「还缺什么信息」+ 建议 48h 冷却 |

### 6.3 是否给替代品

```
给替代，当且仅当：
  confidence == high
  AND primary_need 主要是 utility 或 novelty（novelty 则替代优先「租/体验」）
  AND 当前商品匹配度 != high
  AND 能说出替代如何更好满足同一需求原子

否则：
  不给商品替代；可给「非购物行动」（散步、愿望单冷冻、跟朋友借等）
```

### 6.4 评测如何参与

MVP 输入方式（按优先级）：

1. 用户粘贴评测正文或笔记  
2. 用户粘贴评测链接（当纯文本；MVP **不要求**稳定抓取网页，LLM 仅能基于 URL 字符串则应要求用户改贴正文）  
3. 无评测：用模型常识 + 明确标注「未引入外部评测，置信度封顶 medium」

摘要输出结构（固定 JSON 字段）：

- `pros`: string[≤3]
- `cons`: string[≤3]
- `best_for`: string
- `not_for`: string
- `source_note`: string（用户提供 / 模型常识）

---

## 7. 信息架构（小程序页面）

| 页面路径 | 用途 |
|----------|------|
| `/pages/index/index` | 首页：一句话说明 + 开始冷静 + 冷静清单入口 |
| `/pages/chat/chat` | 对话拆解 + 可选贴评测 |
| `/pages/result/result` | 结果卡：需求 / 判决 / 依据 / 替代 / 置信度 |
| `/pages/list/list` | 本地冷静清单 |
| `/pages/about/about` | 产品理念、隐私说明（本地存储、不上传购物账号） |

视觉气质建议：冷静、留白、少量强调色；**不要**电商大促风、红包风、紫渐变 AI 风。文案短、锋利。

---

## 8. 技术架构建议

### 8.1 推荐栈

| 层 | 选型 | 说明 |
|----|------|------|
| 小程序框架 | 原生微信小程序 或 Taro 3 + React | 若执行者更熟 React 用 Taro；否则原生即可 |
| 样式 | 自写 WXSS / CSS，少依赖 UI 库 | 保持轻 |
| 后端 | 云函数 **或** 极简 Node/Cloudflare Worker | 只负责：组装 prompt、调 LLM、返回结构化 JSON |
| LLM | 任意可用的 Chat API（OpenAI / DeepSeek / 智谱等） | Key 放服务端，禁止写进小程序包 |
| 存储 | `wx.setStorage` | 会话草稿、冷静清单；无登录 |

### 8.2 后端 API（最小）

#### `POST /api/analyze/step`

请求：

```json
{
  "sessionId": "uuid",
  "productText": "想买的 XX",
  "messages": [{"role":"user|assistant","content":"..."}],
  "phase": "clarify_need | confirm_need | review_optional | judge"
}
```

响应（始终 JSON，字段稳定）：

```json
{
  "assistantMessage": "下一句对用户说的话",
  "phase": "clarify_need | confirm_need | review_optional | judge | done",
  "draft": {
    "productName": "",
    "primaryNeedId": "",
    "secondaryNeedId": null,
    "needSentence": "",
    "scene": "",
    "userConfirmedNeed": false
  },
  "result": null
}
```

当 `phase === "done"` 时填充 `result`：

```json
{
  "verdict": "stop | wait | buy | replace",
  "confidence": "high | medium | low",
  "needSentence": "",
  "primaryNeedId": "",
  "matchScore": "high | medium | low",
  "reasons": ["..."],
  "reviewSummary": {
    "pros": [],
    "cons": [],
    "best_for": "",
    "not_for": "",
    "source_note": ""
  },
  "alternatives": [
    {
      "title": "",
      "why": "",
      "servesNeedId": "",
      "type": "product | non_purchase | rent_or_try"
    }
  ],
  "cooldownHours": 48
}
```

`alternatives` 在不该给时返回 `[]`。

### 8.3 Prompt 工程要求

系统提示词必须写入：

- 产品原则全文精简版
- 需求原子枚举
- 判决与替代门槛
- 输出必须可被服务端解析为上述 JSON（可用 tool/function calling 或强制 JSON mode）
- 禁止输出购买链接成交话术、禁止夸大优惠

另备一套 **fallback 规则**：LLM 失败时，用本地问卷分支给出 `wait` + `low` 置信度，保证小程序不白屏。

---

## 9. 分阶段任务（给 GPT 的执行顺序）

### Phase 0 · 工程脚手架（0.5 day）

- [ ] 初始化小程序项目（原生或 Taro）
- [ ] 创建页面路由：index / chat / result / list / about
- [ ] 配置合法域名占位、环境变量示例 `.env.example`（仅服务端）
- [ ] README：如何用微信开发者工具打开、如何配置 API Key

**验收**：开发者工具能编译，四个页面能互相跳转。

---

### Phase 1 · 静态主流程 UI（1 day）

- [ ] 首页文案与 CTA
- [ ] Chat 页：消息列表、输入框、进度提示（如 2/5）
- [ ] Result 页：按设计结构排好「假数据」结果卡
- [ ] List 页：读写 localStorage 的冷静清单
- [ ] About 页：原则说明

**验收**：用 mock JSON 走通「输入 → 对话（假）→ 结果 → 加入清单」。

---

### Phase 2 · 对话状态机（1 day）

- [ ] 前端 session 状态：`phase`、messages、draft
- [ ] 实现与 `/api/analyze/step` 的对接（可先 mock server）
- [ ] 用户确认需求句的 Yes/No 交互
- [ ] 可选「跳过评测」与「粘贴评测」分支

**验收**：不接真 LLM 时，用 mock 也能按 phase 推进到 done。

---

### Phase 3 · 后端 + LLM（1–2 days）

- [ ] 实现云函数或 Node 服务
- [ ] 系统提示词 + JSON schema 校验（可用 zod）
- [ ] 判决后处理：按第 6 节规则 **强制过滤** 不合法的 alternatives
- [ ] 错误处理与 fallback
- [ ] 日志：只打 sessionId 与 phase，不存用户隐私到第三方分析（MVP 可仅 console）

**验收**：用「未来眼镜 / 床上看片」类例子，稳定得到 `stop` 或 `replace`；用「纯虚荣」例子得到 `stop` 且 `alternatives=[]`。

---

### Phase 4 · 文案与体验打磨（0.5–1 day）

- [ ] 损友语气：短句、具体、不爹味
- [ ] 结果卡支持一键复制摘要
- [ ] 空态、加载态、失败态
- [ ] 预置 3 个演示案例按钮（眼镜 / 手机 / 又贵又没用的小家电）

**验收**：其他人不用讲解能独立跑完一次；截图发群看得懂。

---

### Phase 5 · 提测清单（收尾）

- [ ] 真机预览（iOS/Android 各一）
- [ ] 无网络 / API Key 失效时的提示
- [ ] 隐私：确认 Key 不在小程序包内
- [ ] 性能：首屏与对话页无明显卡顿

---

## 10. 演示用例（必须通过）

### Case A · 劝停无替代

- 输入：想买很贵的未来眼镜  
- 用户确认需求：主要想「显得有科技人设」  
- 期望：`verdict=stop`，`alternatives=[]` 或仅 `non_purchase`，`confidence=high|medium`

### Case B · 劝停当前 + 给替代

- 输入：同上  
- 用户确认需求：主要想「躺床上看大屏电影」  
- 可选评测：沉浸好但重、热、内容少  
- 期望：`verdict=replace`，1–2 个替代（投影/平板方案），`servesNeedId=utility`

### Case C · 低置信度

- 用户对确认句说「不对」且补充含糊  
- 期望：`confidence=low`，`verdict=wait`，无商品替代，提示还缺什么 / 48h

### Case D · 可买（少见但要有）

- 输入：干湿分离拖把，租房卫生间经常拖不干净  
- 需求：utility，场景清晰，有正面评测摘要  
- 期望：允许 `buy`，alternatives 可空

---

## 11. 目录结构建议

```
Calm-Buy/
  任务计划书.md          ← 本文件
  README.md
  miniprogram/           ← 小程序前端
    pages/
    components/
    utils/
    app.js / app.json / app.wxss
  server/                ← API（若用云函数则改为 cloudfunctions/）
    src/
      prompt.ts
      schema.ts
      judge.ts           ← 后处理：过滤 alternatives
      index.ts
  mocks/
    case-a.json
    case-b.json
    case-c.json
    case-d.json
```

---

## 12. 给执行 GPT 的工作守则

1. **严格按 Phase 0→5 顺序**，不要一上来就接模型却没有 UI。  
2. **先让 mock 跑通**，再接真 LLM。  
3. **服务端强制执行第 6 节规则**，不要把规则只写在 prompt 里指望模型自律。  
4. 不要扩展 Out of Scope 功能（登录、爬虫、支付等），除非计划书修订。  
5. 所有面向用户的文案用 **简体中文**。  
6. 每完成一个 Phase，在 README 更新「当前进度」与「如何运行」。  
7. 若微信小程序 AppID 缺失：使用测试号 / 游客模式说明，不阻塞开发。  
8. API Key 使用环境变量；提供 `.env.example`，禁止提交真实 Key。

---

## 13. 产品文案种子（可直接用）

**首页标题**：先别买。想清楚你到底要什么。  

**首页副标题**：上头时找我。拆开欲望，参考评测，必要时给你更好的替代——更多时候，劝你省下这笔钱。  

**结果区标题示例**：

- 你真正想要的是  
- 对这件东西的判决  
- 依据  
- 替代方案（若有）  
- 置信度  

**劝停结尾**：把愿望放进冷静清单。48 小时后还想要，再来找我。

---

## 14. 后续可选项（不要进 MVP）

- 自动抓取公开评测（需合规与解析稳定性）
- 拍照识品
- 微信订阅消息：48h 冷静到期提醒
- 个人需求词典（多次会话沉淀主需求分布）
- 与「什么值得买」等内容源合作 API

---

## 15. 交接检查表（人工验收）

- [ ] Case A/B/C/D 全部符合期望  
- [ ] 无 API Key 时有友好 fallback  
- [ ] 结果卡可截图传播  
- [ ] 冷静清单本地可增删  
- [ ] README 可让陌生人 10 分钟内跑起来  

---

**文档版本**：v1.0  
**日期**：2026-07-26  
**状态**：可交付执行
